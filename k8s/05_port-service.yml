apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: overmods
spec:
  selector:
    app: overmods
    service: backend
  type: NodePort
  ports:
    - name: backend
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 31337
